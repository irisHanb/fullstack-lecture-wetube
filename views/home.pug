extends layouts/main
include mixins/videoBlock

block content
  .home-videos     
    each video in videoList
      +videoBlock(video)
      +videoBlock(video)
      +videoBlock(video)
      +videoBlock(video)
      +videoBlock(video)
      +videoBlock(video)
      +videoBlock(video)
      +videoBlock(video)
      +videoBlock(video)
      +videoBlock(video)
      +videoBlock(video)
      +videoBlock(video)
      +videoBlock(video)
      +videoBlock(video)
      +videoBlock(video)
      +videoBlock(video)
      +videoBlock(video)
      
      
